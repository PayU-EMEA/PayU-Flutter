abstract class ApplePayServiceMethod {
  static const authorize = 'authorize';
  static const canMakePayments = 'canMakePayments';

  static const onDidAuthorize = 'onDidAuthorize';
  static const onDidCancel = 'onDidCancel';
}
