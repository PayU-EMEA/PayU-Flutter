import 'package:flutter/material.dart';

class InstallmentsHeaderWidget extends StatelessWidget {
  const InstallmentsHeaderWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
