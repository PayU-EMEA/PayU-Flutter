import 'package:flutter/material.dart';

abstract class PayuIcon {
  static const back = Icons.arrow_back;
  static const creditCard = Icons.credit_card;
  static const lock = Icons.lock_outline;
  static const send = Icons.send;
}
