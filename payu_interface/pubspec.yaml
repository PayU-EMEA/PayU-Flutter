name: payu_interface
description: Flutter package with Payu interface which allows to access the main properties directly.

version: 1.2.1
publish_to: none

homepage: https://github.com/PayU-EMEA/PayU-Flutter
issue_tracker: https://github.com/PayU-EMEA/PayU-Flutter/issues

environment:
  sdk: '>=3.8.0 <4.0.0'
  flutter: ">=3.32.0"

dev_dependencies:
  build_runner: ^2.5.0
  dependency_validator: ^5.0.5
